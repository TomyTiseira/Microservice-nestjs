export enum OrderStatus {
  PENDING = 'PENDING',
  CONFIRMED = 'CONFIRMED',
  CANCELLED = 'CANCELLED',
  PAID = 'PAID',
}

export enum ActionState {
  CONFIRM = 'confirm',
  CANCEL = 'cancel',
  PAY = 'pay',
}
